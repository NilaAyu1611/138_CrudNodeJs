<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212; /* Dark Background */
            color: #f8f9fa; /* White text */
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 600px;
            margin-top: 50px;
            background-color: #1f1f1f; /* Darker container background */
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.6); /* Soft shadow */
        }

        h1 {
            text-align: center;
            color: #00bcd4; /* Accent color */
            margin-bottom: 20px;
        }

        .form-group label {
            color: #b0b0b0; /* Light gray labels */
            font-size: 1.1rem;
        }

        .form-control {
            background-color: #333; /* Dark background for input */
            border: 1px solid #555; /* Lighter border */
            color: #f8f9fa; /* White text in inputs */
            font-size: 1rem;
            border-radius: 5px;
            padding: 10px;
        }

        .form-control:focus {
            border-color: #00bcd4; /* Highlight border on focus */
            box-shadow: 0 0 10px rgba(0, 188, 212, 0.5); /* Glowing effect */
        }

        button.btn-primary {
            background-color: #00bcd4; /* Accent color for button */
            border: none;
            width: 100%;
            padding: 12px;
            border-radius: 5px;
            font-size: 1.1rem;
            cursor: pointer;
        }

        button.btn-primary:hover {
            background-color: #0097a7; /* Darker shade on hover */
        }

        .form-group {
            margin-bottom: 20px;
        }

        textarea.form-control {
            resize: none;
        }

        /* Add a subtle hover effect for form inputs */
        .form-control:hover {
            border-color: #00bcd4;
        }

       /* Footer */
       footer {
        text-align: center;
        background-color: #1f1f1f; /* Footer background */
        color: #ffff04;
        padding: 10px 0;
        font-size: 0.9rem;
        position: fixed;
        bottom: 0;
        width: 100%;
        }


        footer a {
            color: #0df8dd;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: #cc790b;
        }

    </style>
</head>
<body>

    <div class="container">
        <h1>Contact Us</h1>
        <form>
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" class="form-control" id="name" required>
            </div>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" class="form-control" id="email" required>
            </div>
            <div class="form-group">
                <label for="message">Message:</label>
                <textarea class="form-control" id="message" rows="5" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>

    <footer>
        <p>&copy; 2024 My Website. Join Us.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
